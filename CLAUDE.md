<!-- Maintainer : garder ce fichier court (< 200 lignes). La connaissance détaillée vit dans
docs/revenu-disponible.md ; ne PAS l'importer ici via @ (un import se charge en entier à chaque
session). Ce bloc de commentaire est retiré du contexte avant injection : coût nul. -->

# CLAUDE.md — Reconstruction « Revenu disponible » (MFQ)

Reconstruction **lisible, vérifiée et testée** du calculateur du ministère des Finances du Québec,
à partir du fichier minifié (dé-minifié dans `reference/`). **Un poste fiscal à la fois.**

## Au début de chaque session
1. Lis `docs/revenu-disponible.md` — la référence : méthode, sources, **§5 tableau d'avancement**, algorithmes commentés.
2. Identifie le **prochain poste non terminé** dans §5 (et le pointeur en fin de fichier, §6).
3. Lance `npm test` pour confirmer l'état de départ (doit être vert).

## Méthode pour chaque poste
1. **Tracer** la sortie dans `calc(data)` de `reference/revenu-disponible_dec2025_beautified.js`, ancré sur les **noms de cellules** (jamais les numéros de ligne, instables). Remonte la chaîne `data['X'] = cY = …` au `grep`.
2. **Extraire** les paramètres : colonne **`M` = 2025**, colonne **`L` = 2026**.
3. **Confronter CHAQUE valeur** à la source officielle (Revenu Québec, ARC, Service Canada, Retraite Québec, MFQ, BSIF) et **citer la disposition de loi**.
4. **Vérifier numériquement** : reproduire les maximums / points de contrôle officiels, puis **ajouter des assertions** dans `tests/maximums.test.ts`.
5. **Écrire l'algorithme épuré** dans `src/postes/NN-nom.ts`, cohérent avec le socle ; **ré-exporter** depuis `src/index.ts`.
6. Marquer **`⚠️ à vérifier`** toute valeur non confirmée.
7. **Mettre à jour `docs/revenu-disponible.md`** : tableau des sources (§2), tableau d'avancement (§5), nouvelle section du poste, pointeur du **prochain poste** (§6).
8. Lancer `npm test` **et** `npm run typecheck` avant de conclure — les deux doivent passer.

## Conventions du code source (fichier MFQ)
- Comparaison **2025** (col. `M`, suffixe sortie `_old`, cellules de calcul col. **`T`**) vs **2026** (col. `L`, `_new`, col. **`S`**).
- Les cotisations sont stockées en **négatif** (elles réduisent le revenu disponible) → les fonctions TS les renvoient en **positif**.
- Table des situations (rangées B5:G9) : col. C = `nbAdultes`, col. G = `retraité` ; le drapeau `c2G11 == 1` ⟺ ménage retraité.
- `reference/` est en **lecture seule** (traçage uniquement). Ne jamais l'éditer, ni exécuter le `.js`.

## Conventions du code TypeScript
- `Annee = 2025 | 2026`. Paramètres par année dans un `Record<Annee, …>` exporté (ex. `RRQ`, `AE`, `FSS`).
- Calcul **par adulte**, puis somme sur le ménage via le helper `revenusAdultes(menage)` du socle.
- Réutiliser `src/socle.ts` (types, `Situation`, `SITUATIONS`, `impotProgressif`, `credit`). **Aucune dépendance externe** dans `src/`.
- En-tête de chaque fichier de poste : sortie(s) du code, base légale, sources (`Sn`) — comme les postes 01 à 04.

## Règles absolues (non négociables)
- **Ne rien présumer.** Aucune valeur ni comportement « de mémoire » : seulement ce qui est **tracé dans le code** *et* **vérifié dans une source officielle**.
- Pour le **texte de loi** : partir de l'**URL officielle précise** (`laws-lois.justice.gc.ca`, `canlii.org`, `legisquebec.gouv.qc.ca`) et citer **verbatim**. `WebFetch` *résume* le contenu — viser la source exacte plutôt qu'une recherche large.
- Tout élément non confirmé reste marqué **`⚠️ à vérifier`** ; ne jamais « combler » par une estimation.
- Ne jamais affaiblir la suite de tests pour la faire passer : un test rouge = un écart réel avec la source officielle, à investiguer.

## Style de réponse
- **Français**, bref, précis, pédagogique, en **markdown**.
- Montrer le **traçage** (chaîne de cellules) et la **confrontation aux sources** (tableau) quand c'est utile.

## Commandes
```bash
npm test            # vitest — doit rester vert
npm run typecheck   # tsc --noEmit
```

## État
**Reconstruction complète : postes 1 → 20 + orchestrateur, tous vérifiés par parité.** `calculerRevenuDisponible(menage, annee)` (poste 20) enchaîne tous les postes depuis les entrées brutes, reconstruit les bases de revenu internes (revenu net familial QC + AFNI fédéral + revenu AL) et reproduit `RD` à la cent. L'orchestrateur expose aussi le **détail par poste** (`detail`), vérifié par parité.

**Application Web.** Stack : Next.js 16 (App Router, **webpack** — `dev`/`build` avec `--webpack`), TypeScript strict, Tailwind 4, shadcn/ui sur Radix. Moteur dans `src/postes…` ; app dans `src/app` + `src/components` + `src/lib`, alias `@/* → src/*`. Le **calcul du revenu disponible** reste **100 % client** ; le back-end sert aux **comptes/sauvegarde** (Phase 5 : Better Auth + Prisma + Postgres) **et** aux **routes de l'assistant IA** (qui réexécutent le moteur côté serveur pour outiller le modèle). `npm run dev` sur le **port 3100** (3000 occupé par le site Docusaurus de l'utilisateur).
- ✅ **Phase 1 (MVP)** : formulaire (5 situations, âges, revenus, enfants) + tableau **2025 vs 2026** ventilé par poste avec sous-totaux et écart.
- ✅ **Phase 2 (pédagogie, bilingue FR/EN)** : i18n léger (`src/lib/i18n.ts`), registre par poste (`src/lib/postes-info.ts` — nom, description, objectif, règle, références en FR+EN), sélecteur de langue, panneaux `Dialog` « En savoir plus » par poste.
- ✅ **Phase 3 (taux marginaux implicites)** : `src/lib/taux-marginal.ts` (TMI = `1 − dRD/dr` par différences finies sur le revenu, décomposé par catégorie ; `zonesTrappe` seuil 60 %) + graphique d'aires empilées Recharts via shadcn (`src/components/graphique-taux-marginal.tsx`), avec courbe cumulative et repère au revenu actuel. **Façon Laferrière/CQFF** (juin 2026) : ligne pointillée **« barème seul »** (`tauxBareme` — taux du palier au revenu imposable de l'adulte 1, zéro sous le BPA, fédéral ×0,835), **zones > 100 %** en marquage foncé, note **FERR** pour les retraités (la courbe = coût marginal d'un retrait) ; méthode documentée au guide (§TEMI, tableau `exemples/temi.tex` généré). Alias `@` ajouté à `vitest.config.ts` (le générateur importe `src/lib/taux-marginal`). **Seuils** (`src/lib/seuils.ts` + carte `seuils-bascule.tsx` sur le calculateur) : position nette (transferts − impôts, hors cotisations/frais de garde), 1ᵉʳ $ d'impôt QC/féd et bascule « contributeur net » (ensemble + par gouvernement), balayage + dichotomie au dollar sur le moteur ; ligne de revenu annotée ; outil assistant `seuils` ; §seuils au guide avec tableau des 13 ménages généré/vérifié (invariant de dichotomie).
- ✅ **Phase 4a (moteur paramétrable)** : `src/parametres.ts` — type `Parametres` (tous les postes) + `PARAMETRES_OFFICIELS`. Les fonctions acceptent `Annee | Parametres` (numéro ⇒ officiel ; bundle ⇒ sur mesure). Permet de modéliser des changements de paramètres (budget).
- ✅ **Phase 4b (comparaison)** : route `/comparaison` — deux scénarios (ménage + année) côte à côte, écart par poste. Composants réutilisables extraits (`formulaire-menage.tsx`, `tableau-resultats.tsx`, `menage-etat.ts`).
- ✅ **Phase 4c (éditeur de paramètres)** : route `/budget` — comparer **deux jeux de paramètres** (scénarios A et B, onglets) sur un **même ménage** ; chaque scénario a son année de base et ses modifications (montants, taux, seuils, **paliers**), écart par poste. Par défaut A = officiel 2025, B = officiel 2026. `editeur-parametres.tsx` (Accordion générique sur le bundle), `parametres-meta.ts` (libellés). Bundle modifiable = `structuredClone(PARAMETRES_OFFICIELS[base])`. *(`/comparaison` = comparer des **ménages** ; `/budget` = comparer des **paramètres**.)*
- ✅ **Partage par URL** : `src/lib/partage.ts` (encode l'état dans `?s=`, base64url ; pour les paramètres on n'encode que les **écarts** vs officiel → URL courte), `use-partage-url.ts` (hook : charge au montant, synchronise ensuite), `bouton-partage.tsx` (« Copier le lien »). Sur les 3 pages.
- ✅ **Déployé sur Vercel** : **https://revenu-disponible.vercel.app/**, déploiement continu à chaque fusion dans `main`.
- ✅ **Phase 5 (comptes + sauvegarde)** : Better Auth (courriel/mot de passe + Google optionnel) + Prisma **7** (générateur `prisma-client` → `src/generated/prisma` ; runtime via `@prisma/adapter-pg`, URL CLI dans `prisma.config.ts`) + PostgreSQL. Schéma `prisma/schema.prisma` (User/Session/Account/Verification + `Scenario`). Persistance par **Server Actions** (`src/lib/scenarios.ts`, validées Zod, contrôle de session). UI : `src/lib/auth.ts`/`auth-client.ts`, route `app/api/auth/[...all]`, `src/components/compte/` (auth-dialog, barre-compte, scenarios-dialog, bouton-enregistrer). Un scénario sauvegardé stocke le **code de partage** ; « Ouvrir » navigue vers la page avec `?s=` (réutilise le décodage). **Dev local** : Postgres Homebrew, base `revenu_disponible`. **Build** : `serverExternalPackages` pour `kysely`/adaptateurs (sinon erreurs webpack) ; `react-is` ajouté en dép. directe ; `.npmrc legacy-peer-deps`. Le build **réussit sans variables d'env** (fonctions de compte dormantes jusqu'à config Neon).
- ✅ **Activé en prod** : base **Neon** (`neondb`, AWS US East 1), variables Vercel (`DATABASE_URL` **pooled** `-pooler`, `BETTER_AUTH_SECRET`, `BETTER_AUTH_URL`), migration `init` appliquée. Comptes + sauvegarde **en ligne et vérifiés** (`get-session` → 200 ; `sign-in` interroge Neon → 401).
- ✅ **Google OAuth configuré** (local + prod) : `GOOGLE_CLIENT_ID/SECRET` + `NEXT_PUBLIC_GOOGLE_ENABLED=true` (le bouton n'apparaît que si ce drapeau public = `true` ; **inliné au build** → redéploiement requis). Callbacks Google Cloud : `…/api/auth/callback/google` pour `localhost:3100` **et** la prod. Liaison auto courriel↔Google via `account.accountLinking` = `{ trustedProviders:["google"], requireLocalEmailVerified:false }` (sinon `account_not_linked` : Better Auth exige le courriel local vérifié pour lier, or la vérif. courriel est désactivée — **POC** ; en prod réelle, activer la vérif. courriel plutôt que ce drapeau).
- ✅ **Bibliothèque** (`/bibliotheque`, connexion requise) : crée/gère des **ménages types** et des **jeux de paramètres** réutilisables. Tables Prisma `Menage` (data JSON = `MenageEtat`) et `JeuParametres` (`anneeBase` + data JSON = **écarts** vs officiel) ; migration `bibliotheque` (**à appliquer sur Neon** : `DATABASE_URL="<neon-direct>" npx prisma migrate deploy`). Server Actions `src/lib/bibliotheque.ts` (Zod + session). `/budget` = **1 ménage + 2 jeux** (sélecteurs : officiels + bibliothèque, + « Enregistrer ce jeu/ménage ») ; `/comparaison` = **1 jeu + 2 ménages**. Sélecteurs `MenagePicker`/`JeuPicker`, dialogues `menage-dialog`/`jeu-dialog` (réutilisent `FormulaireMenage`/`EditeurParametres`). `diffParams`/`appliquerParams` exposés depuis `partage.ts`. Pages utilisables **sans compte** (jeux officiels).
- ✅ **Frais de garde par enfant** (alignement MFQ) : saisie par enfant **âge + frais + service de garde** (subventionné/non, plafond **15 000 $/enfant**) dans `FormulaireMenage`. `MenageEtat.enfants: EnfantEtat[]` (remplace `agesEnfants` ; `normaliserMenageEtat` gère la rétro-compat des ménages déjà sauvegardés). **Trois effets sur le RD, vérifiés par parité** (679 tests, dont 5 cas frais > 0) : (1) **crédit QC** — frais **non subventionnés** seulement ; (2) **déduction fédérale** (`06-garde.deductionFraisGardeFederale`, plafonds **agrégés** 8000 ≤5 ans / 5000 6-15 / 0 ≥16, borne ⅔ du revenu de travail du conjoint le moins payé, frais subv. **et** non subv.) → réduit l'AFNI (→ ACE/TPS) **et** le revenu net fédéral imposable (`19-impot`) ; (3) **coût** total des frais soustrait du RD (`composantes.fraisGarde`, `detail.fraisGardeCout`).
- ✅ **Design (éditorial, aligné shadcn.com)** : thème shadcn personnalisé dans `globals.css`. **Polices Geist** pour **titres ET texte** (comme ui.shadcn.com — pas de serif) + chiffres tabulaires. Accent **bleu ardoise** (`oklch(0.47 0.15 256)`) sur boutons/liens/focus + **filet bleu** en haut de page (signature, `h-1 bg-primary` dans `layout`). **Contraste/relief** : fond de page **gris-froid** (`--background oklch(0.969 …)`) → cartes blanches détachées (ombre douce sur `Card` + bords plus nets), rayon **8 px**, bloc-héros RD en carte blanche. **Palette de graphiques froide** (`chart-1…5` bleus/sarcelle → courbe de taux marginal lisible). Tableaux : **écart vert (gain) / rouge (perte)** + « + ». **Mode clair/sombre** (`next-themes` ; `ThemeProvider` dans `layout`, bascule `ThemeToggle` dans l'en-tête des 4 pages). 100 % shadcn (variables CSS + composants).
- ✅ **Refonte « espace de travail » (façon Obsidian)** : pleine largeur, **trois volets redimensionnables** (`react-resizable-panels` via shadcn `resizable`) — entrées | résultats | **panneau d'info toujours ouvert** — en-têtes de volets uniformes (16 px, filets alignés), **pied de page** pleine largeur (note technique + crédits + lien GitHub). Composant partagé **`EspaceTravail`** (`src/components/espace-travail.tsx`) + **`PanneauInfoVolet`/`usePanneauInfo`** (`panneau-info.tsx`) sur les **4 pages**. Le **panneau d'info** (ex-modales ⓘ) montre description/objectif/règle/références + lien vers la source officielle (`sources-postes.ts`). Remplace la disposition en **cartes centrées** : les volets sont désormais **plats** (le reste du thème — Geist, accent ardoise, mode sombre, palette — tient). `/bibliotheque` en trois volets (ménages | jeux | **Aide**).
- ✅ **Assistant IA (Claude) — BYOK** : clé Anthropic **fournie par l'utilisateur** (hook `useCleApi`, `localStorage`, envoyée à la requête, **jamais conservée côté serveur** → aucune `ANTHROPIC_API_KEY` serveur, rien à configurer sur Vercel ; voir mémoire `assistant-ia-byok`). Stack : **Vercel AI SDK v6** (`ai`, `@ai-sdk/anthropic`, `@ai-sdk/react`) + **Sonnet 4.6**, streaming, **tool-calling ancré sur le moteur** ; rendu markdown (`react-markdown` + `remark-gfm`). Volet droit (bascule Détail/Aide ⇄ Assistant), composant générique `AssistantChat`. **Route `/api/assistant`** (calculateur, **clé seule, sans connexion**) : explique le résultat, « et si… ? », et **`detail_poste`** (règle, références, **paramètres officiels**, montant exact) ; **`code_poste`** (le modèle **lit le code source TS** d'un poste — mécanique exacte ; correspondance fixe clé→fichier dans `src/lib/code-postes.ts`, clés spéciales `socle`/`orchestrateur`, sources incluses au bundle Vercel via `outputFileTracingIncludes`) ; **ventilation exacte par poste** injectée dans le contexte avec **interdiction de recalculer** (le modèle recopie les chiffres). **Route `/api/assistant-bibliotheque`** (**connexion + clé**) : crée ménages types / jeux de paramètres **par description** via les Server Actions (rafraîchit les listes).
- ✅ **Guide PDF** (`docs/guide/`, LaTeX/latexmk, 44 p., lié au README et servi via `public/guide-revenu-disponible.pdf` — recopier après compilation) : description/objectif/règle/paramètres/références par poste + **exemples chiffrés générés** : `scripts/exemples-guide.ts` calcule pas à pas 3-5 ménages types (cast M1–M13, année 2025) par poste, **vérifie chaque montant par assertion contre le moteur** et émet `docs/guide/exemples/*.tex` (`npm run exemples` ; test de fraîcheur `tests/exemples-guide.test.ts` — si un paramètre du moteur change, régénérer puis recompiler le PDF). En LaTeX, **pas d'accents nus en mode math** (cmr sans glyphes) : envelopper de `\text{…}`.
- Prochaines phases : **extensions hors-MFQ** ; raffinements de l'assistant (créer des jeux avec modifications de paramètres). *(Note : libellés de champs de l'éditeur « humanisés » depuis les noms français des paramètres — pas encore traduits par champ.)*

**Toujours lancer `npm test` + `npm run typecheck` ; le build se vérifie avec `npm run build`.**
